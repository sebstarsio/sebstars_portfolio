# Archive — Fichiers historiques / non utilisés

Ce dossier contient des fichiers retirés du flux principal du projet mais conservés pour référence.

- **scripts/** : Anciens scripts de capture (Puppeteer/Playwright), post-traitement d’assets, diagnostic. Les commandes npm associées ont été retirées de `package.json`.
- **images/projects/** : Anciennes images projet (`*-thumb.png`, `*-detail.png`) et sorties du pipeline `processed/`. Le site utilise désormais uniquement les `.svg` référencés dans `src/lib/projects.ts`.

Date d’archivage : 2025-03.
